apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: sample-api
  labels:
    app: sample-api
spec:
  selector:
    app: sample-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
